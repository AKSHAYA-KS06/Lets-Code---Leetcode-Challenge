class Solution:
    def averageValue(sef,nums:list[int])->int:
        sum=0
        count=0
        for i in nums:
            if i%6==0:
                sum=sum+i
                count=count+1
          if count==0:
              return 0
          return int(sum/count)
